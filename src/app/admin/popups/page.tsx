"use client";

import { useEffect, useState, type FormEvent, type ChangeEvent } from "react";

const emptyForm = { name: "", type: "MODAL", target: "GLOBAL", trigger: "PAGE_LOAD", delayMs: 0, frequency: "EVERY_TIME", status: "DRAFT", html: "<h2>Welcome</h2><p>Add your popup content.</p>", css: "" };

type PopupForm = typeof emptyForm;

export default function PopupsPage() {
  const [popups, setPopups] = useState<Record<string, any>[]>([]);
  const [form, setForm] = useState<PopupForm>(emptyForm);
  const [editing, setEditing] = useState<number | null>(null);
  const [saving, setSaving] = useState(false);

  async function load() { const response = await fetch("/api/popups"); const payload = await response.json(); setPopups(payload.data || []); }
  useEffect(() => { load(); }, []);
  function update(key: keyof PopupForm, value: string | number) { setForm((current) => ({ ...current, [key]: value })); }
  async function save(event: FormEvent<HTMLFormElement>) { event.preventDefault(); setSaving(true); await fetch(editing ? `/api/popups/${editing}` : "/api/popups", { method: editing ? "PATCH" : "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(form) }); setForm(emptyForm); setEditing(null); setSaving(false); await load(); }
  async function action(id: number, actionName: string) { await fetch(`/api/popups/${id}`, { method: actionName === "delete" ? "DELETE" : actionName === "duplicate" ? "POST" : "PATCH", headers: { "Content-Type": "application/json" }, body: actionName === "duplicate" ? JSON.stringify({ action: "duplicate" }) : JSON.stringify({ status: actionName }) }); await load(); }
  const setField = (key: keyof PopupForm) => (event: ChangeEvent<HTMLInputElement | HTMLSelectElement | HTMLTextAreaElement>) => update(key, key === "delayMs" ? Number(event.target.value) : event.target.value);

  return <main className="flex flex-col gap-6 p-6 lg:p-8"><div><h1 className="text-2xl font-semibold">Popups</h1><p className="text-muted-foreground">Create and manage targeted website popups.</p></div><div className="grid gap-6 xl:grid-cols-[1fr_420px]"><section className="rounded-xl border bg-card p-6"><h2 className="mb-4 text-lg font-semibold">Popup library</h2><div className="flex flex-col gap-3">{popups.length === 0 && <p className="text-sm text-muted-foreground">No popups yet.</p>}{popups.map((popup) => <div key={popup.id} className="flex items-center justify-between gap-4 rounded-lg border p-4"><div><p className="font-medium">{popup.name}</p><p className="text-sm text-muted-foreground">{popup.type} · {popup.trigger}</p></div><div className="flex items-center gap-2"><span className="rounded-full bg-muted px-2 py-1 text-xs">{popup.status}</span><button className="rounded border px-2 py-1 text-xs" onClick={() => { setEditing(popup.id); setForm({ ...emptyForm, ...popup }); }}>Edit</button><button className="rounded border px-2 py-1 text-xs" onClick={() => action(popup.id, popup.status === "ACTIVE" ? "PAUSED" : "ACTIVE")}>{popup.status === "ACTIVE" ? "Pause" : "Enable"}</button><button className="rounded border px-2 py-1 text-xs" onClick={() => action(popup.id, "duplicate")}>Duplicate</button><button className="rounded border px-2 py-1 text-xs" onClick={() => action(popup.id, "delete")}>Delete</button></div></div>)}</div></section><section className="rounded-xl border bg-card p-6"><h2 className="mb-4 text-lg font-semibold">{editing ? "Edit popup" : "Create popup"}</h2><form onSubmit={save} className="flex flex-col gap-4"><input className="h-9 rounded-md border bg-background px-3 text-sm" placeholder="Popup name" value={form.name} onChange={setField("name")} required /><div className="grid grid-cols-2 gap-3"><select className="h-9 rounded-md border bg-background px-3 text-sm" value={form.type} onChange={setField("type")}><option value="MODAL">Modal</option><option value="BAR">Bar</option><option value="ELEMENT">Element</option></select><select className="h-9 rounded-md border bg-background px-3 text-sm" value={form.trigger} onChange={setField("trigger")}><option value="PAGE_LOAD">Page load</option><option value="DELAY">Delay</option><option value="ELEMENT_CLICK">Element click</option></select></div><input className="h-9 rounded-md border bg-background px-3 text-sm" type="number" min="0" placeholder="Delay in milliseconds" value={form.delayMs} onChange={setField("delayMs")} /><textarea className="min-h-32 rounded-md border bg-background p-3 text-sm" value={form.html} onChange={setField("html")} placeholder="Popup HTML" /><textarea className="min-h-24 rounded-md border bg-background p-3 text-sm" value={form.css} onChange={setField("css")} placeholder="Popup CSS" /><div className="flex gap-2"><button className="rounded-md bg-primary px-4 py-2 text-sm text-primary-foreground" type="submit" disabled={saving}>{saving ? "Saving…" : editing ? "Update popup" : "Create popup"}</button>{editing && <button className="rounded-md border px-4 py-2 text-sm" type="button" onClick={() => { setEditing(null); setForm(emptyForm); }}>Cancel</button>}</div></form></section></div></main>;
}
